package repository;

import model.entity.PagoVisita;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PagoVisitaRepository extends JpaRepository<PagoVisita,Integer> {
}
